const firstAngle = 80;
const secondAngle = 80;
const thirdAngle = 20;
let addAngle = 0;

addAngle = firstAngle + secondAngle + thirdAngle;

if (firstAngle <= 0 || secondAngle<=0 || thirdAngle<=0) {
    console.log("Angulo inserido inválido");
} else if (addAngle == 180) {
    console.log(addAngle == 180);
} else {
    console.log(addAngle == 180);
}